# Azure DevOps Integration Settings
# Copy this file to .env and fill in your values

# ============================================
# AZURE DEVOPS CONFIGURATION
# ============================================

# Your Azure DevOps organization name (from https://dev.azure.com/{org})
AZURE_DEVOPS_ORG=your-organization

# The project name within your organization
AZURE_DEVOPS_PROJECT=your-project

# Personal Access Token (PAT) with Work Items: Read & Write scope
# Create at: https://dev.azure.com/{org}/_usersSettings/tokens
# Required scopes: Work Items (Read & Write)
AZURE_DEVOPS_PAT=your-personal-access-token

# ============================================
# CUSTOM FIELD NAMES
# ============================================
# These are the field reference names for your "Event Summary Log" work item type
# Find your field names at: Organization Settings > Process > Your Process > Event Summary Log

# Field containing feedback HTML with embedded images
# Default: Custom.Feedback
AZURE_DEVOPS_FEEDBACK_FIELD=Custom.Feedback

# Field containing the event date
# Default: Custom.EventDate  
AZURE_DEVOPS_EVENTDATE_FIELD=Custom.EventDate

# ============================================
# DOMAIN ALLOW-LIST
# ============================================

# Comma-separated list of email domains that are auto-provisioned on first login.
# Users from these domains get role 'user' automatically without admin adding them.
# Example: spektrasystems.com,contoso.com
ALLOWED_DOMAINS=spektrasystems.com

# ============================================
# STORAGE SETTINGS
# ============================================

# Storage mode: 'blob' for Azure Blob Storage, 'local' for local file system
STORAGE_MODE=local

# Azure Blob Storage connection string (required if STORAGE_MODE=blob)
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string

# ============================================
# RMP REQUEST IMPORTS
# ============================================
# Enables onboarding/TTT/custom/localization request imports and shows
# request-imported labs. Paused September 8, 2026; restored September 16, 2026.
# Actual Catalog releases and Content Release Date filters are independent and
# stay enabled either way.
# Explicit opt-in that doubles as a kill switch: must be exactly "true".
# Set it to false and restart to hide and pause imports again, without data loss.
RMP_REQUEST_IMPORTS_ENABLED=true

# ============================================
# AI ANALYSIS (GitHub Models / Azure OpenAI)
# ============================================

# API key for AI analysis — use a GitHub PAT (with Models access) or Azure OpenAI key
# AI_API_KEY=your-github-token-or-azure-openai-key

# AI endpoint (default: GitHub Models inference)
# AI_ENDPOINT=https://models.inference.ai.azure.com

# Model name (default: gpt-4o)
# AI_MODEL=gpt-4o
